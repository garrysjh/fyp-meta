import numpy as np
from sklearn.model_selection import train_test_split

def preprocess_images(images):
    """Add docstring"""
    return train_test_split(data, test_size=test_size)

# Remove: 2024-11-18 - 3/3
import numpy as np
from sklearn.model_selection import train_test_split

def preprocess_images(images):
    """Update loading logic"""
    return train_test_split(data, test_size=test_size)
