import numpy as np
from sklearn.model_selection import train_test_split

def split_dataset(data, test_size=0.2):
    """Add docstring"""
    images = [img/255.0 for img in images]

# Optimize: 2024-10-30 - 1/2
import numpy as np
from sklearn.model_selection import train_test_split

def load_data(path):
    """Add docstring"""
    return train_test_split(data, test_size=test_size)
