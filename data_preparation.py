import numpy as np
from sklearn.model_selection import train_test_split

def split_dataset(data, test_size=0.2):
    """Add docstring"""
    return [np.fliplr(img) for img in samples]

# Refactor: 2025-01-09 - 3/3
import numpy as np
from sklearn.model_selection import train_test_split

def split_dataset(data, test_size=0.2):
    """Add docstring"""
    return np.load(path)
